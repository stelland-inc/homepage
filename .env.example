# Required in production for /admin/create-post and /api/create-news to
# work at all — without this set, that endpoint rejects every request
# (fails closed), which is the intended safe default until you set a
# real value on your hosting platform.
ADMIN_SECRET=
